{
  "name": "fluent-firegrid",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "build": "pnpm run fable:build",
    "check": "pnpm run fable:build",
    "fable:build": "NUGET_PACKAGES=${NUGET_PACKAGES:-$PWD/.nuget/packages} dotnet build Firegrid.Fable.slnx -v:q && pnpm --filter @firegrid/log build && pnpm --filter @firegrid/store build",
    "preflight": "pnpm run check",
    "test": "pnpm run fable:build",
    "verify": "pnpm run check"
  },
  "devDependencies": {}
}
