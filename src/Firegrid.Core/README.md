# @firegrid/core

Fable-native port of the Firegrid core package: the durable workflow engine,
`createWorkflow`/`createMiddleware` builders, cross-version registry, in-memory
run store, request parsing, and the unified `WorkflowEvent` log/transport model.

Public surface lives in `Firegrid.Core.Exports`.
