# @firegrid/fluent

Fable-native EffSharp port of the Restate-like fluent API over the Firegrid
TanStack/S2 durable runtime. Folds in the `clients.ts` surface. Depends on
`@firegrid/core` and `@firegrid/runtime`.
