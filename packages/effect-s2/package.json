{
  "name": "effect-s2",
  "version": "0.0.0",
  "description": "Fable-native EffSharp bindings for S2 durable streams.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/generated/src/Effect.S2/S2.js",
  "exports": {
    ".": "./src/generated/src/Effect.S2/S2.js"
  },
  "files": [
    "Effect.S2.fsproj",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "NUGET_PACKAGES=${NUGET_PACKAGES:-$PWD/../../.nuget/packages} dotnet tool run fable -- Effect.S2.fsproj -o src/generated --optimize --noCache",
    "diagnostics": "echo 'effect-s2 diagnostics are covered by dotnet build'",
    "lint": "echo 'effect-s2 lint is covered by F# compile'",
    "smoke": "bash ./smoke/import.sh",
    "test": "pnpm run smoke",
    "typecheck": "NUGET_PACKAGES=${NUGET_PACKAGES:-$PWD/../../.nuget/packages} dotnet build Effect.S2.fsproj -v:q"
  },
  "dependencies": {
    "@s2-dev/streamstore": "0.24.1"
  }
}
